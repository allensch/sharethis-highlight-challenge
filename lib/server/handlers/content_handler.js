import Debug from 'debug'

const debug = Debug('content-handler')

export default class ContentHandler {

    // static async find(request, response) {
    //     try {
    //         response.json()
    //         debug('response OK', `${}`)
    //     } catch(error) {
    //         response.status(400).send(error)
    //         debug('response ERROR', error)
    //     }
    // }

}
